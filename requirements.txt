streamlit>=1.35.0
pandas>=2.0.0
pydantic>=2.0.0
pygrok>=1.0.0
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354

# NER — install at least one of the following:
# Option A: HanLP (recommended, better Chinese NER)
#   pip install hanlp
#   Models download automatically on first run (~300 MB)
#
# Option B: spaCy small model (lightweight fallback)
#   pip install spacy
#   python -m spacy download zh_core_web_sm
